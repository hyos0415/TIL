function myFUnc() {
    // 1. console에 사과를 출력
   // console.log($('#apple').text());
    // 2. console에 참외 출력
    //console.log($('ul > [class=myList] ').text());
    //아니면 console.log($('#pineapple + li').text());
    // 3. uId라는 값을 찾아서
    //console.log($('[type=text]').attr('id','htha'));
    // 4. 입력상자를 찾아서 사용자가 입력한 값을 alert으로 출력
    //alert($('[type=text]').val());
    // val 안에 값을 넣으면 입력창에 값으로 변경
    //$('[type=text]').val('메시');
    // 5. 고양이 출력 > 특수 선택자 사용
    //console.log($('ol > li:first').text()); ol의 li의 처음
    //console.log($('ol > li:last').text()); ol의 li의 마지막
    //console.log($('ol > li:eq(1)' ).text()); eq(순번)




}